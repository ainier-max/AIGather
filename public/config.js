window.config = {
    // Google Satellite (s=satellite only, y=hybrid with labels)
    mapUrl: 'https://mt1.google.com/vt/lyrs=s&x={x}&y={y}&z={z}',
    // Google Satellite (s=satellite only, y=hybrid with labels)
    //mapUrl: 'https://mt1.google.com/vt/lyrs=y&x={x}&y={y}&z={z}'
    // 高德地图
    //mapUrl: 'https://webst04.is.autonavi.com/appmaptile?style=6&x={x}&y={y}&z={z}'
    //wsUrl:'ws://192.168.25.43:8087/api/ai/chat'//java接口
    wsUrl:'ws://127.0.0.1:8088/api/ai/chat'//python接口
};
